# 桌面版验证记录

## v1.3.0：独立安装、项目团队与脚本入口修复

2026-09-19，Codex 单代理完成。50 项桌面测试均通过，其中 2 项真实启动集成测试单独执行；
启动器安全测试 17 项通过，另 1 项因源码目录未装依赖而跳过，其真实启动路径由前述集成测试覆盖。

- 添加项目异步检查 Node 版本、工具包与依赖；缺少依赖或需要更新时不允许直接启动。
- 安装队列没有 Start 阶段；启动队列没有安装阶段；安装、取消、失败、完成反馈均验证。
- 卸载仅删除 Skill 内的 node_modules，保留项目依赖和用户文件；运行中后台及目录联接阻断删除。
- 从真实 v1.1.0 包安装临时项目，再用新版卸载依赖、更新到 v1.3.0，原项目文件和保留的依赖未变化。
- PowerShell 5.1 / 7 实际启动临时非 Git 项目：旧 REMOTE_TO_DSH_HOME 不被写入，默认供应商与模型正确。
- 桌面路径含中文、空格和方括号；脚本直接启动覆盖中文和空格。启动后调用方正常返回，输出管道不被后台持有。
- 两个脚本重复启动均复用同一个 PID/记录；冲突目标明确拒绝；记录缺失而后台仍运行时不另开团队。
- 用户源配置与旧 DSH Home 逐文件直接内容比较保持不变；全部测试使用假凭据，未发起付费模型请求。
- Skill 元数据检查通过；原有 worktree 保持干净，历史发布标签不变。
- 最终 ZIP 共 281 个条目、39,393,700 字节；完整解压后在仅保留 Windows 系统 PATH 的环境中启动 EXE，通过离线界面验证。
- 五张界面截图覆盖在线工作台、配置、离线模型预览、安装状态与白底弹窗；随附桌面源码、启动脚本与当前源文件直接字节比较一致。

真实启动输入用 DESKTOP_TEST_DSH_MODULES 指定已有固定版本依赖；升级测试用
DESKTOP_TEST_TOOLKIT_PACKAGE 与 DESKTOP_TEST_PREVIOUS_PACKAGE 指定新旧完整包。
测试不修改用户正在使用的 Monitor，不安装或升级共享 Python 环境，不计算校验摘要。


## v1.2.2：配置发现、启动与浅色界面

2026-09-19，41 项常规自动测试通过；另行执行的真实启动集成测试通过，共 42 项。
继续使用 Codex 单代理，原 `payload/`、`install/`、`installer/`、`uninstaller/` 未修改。

- 自动查找在后台执行；唯一结果自动保存，多份配置明确选择，取消时保留原配置。
- 上次选择失效时不自动换供应商；运行副本不能作为用户配置来源。
- 未连接时显示本地模型预览，应用按钮有原因提示和连接入口；旧版连接明确提示限制。
- 模拟深色系统调色板，配置弹窗、消息框、目录浏览器仍为白底。
- Windows PowerShell 5.1 / 7 下验证所选配置、专用运行目录和含方括号路径的进程启动。
- 用临时项目、假凭据和本机已有的固定版本依赖执行真实启动：创建 ACP profile、同步配置、连接 Monitor、读取正确默认模型，最后停止并释放日志。
- 源配置与旧 `REMOTE_TO_DSH_HOME` 目录内容保持原样，未发起模型任务。没有停止用户正在使用的 Monitor。
- 项目依赖齐全时不进入下载阶段；缺少时提示 npm 准备，进度条在各阶段重新显示状态。

真实启动集成测试通过 `DESKTOP_TEST_DSH_MODULES` 指定本机已有的固定版本 `node_modules`，
复制到临时项目后验证，不依赖联网下载；不设置时此项跳过。完整安装测试仍使用
`DESKTOP_TEST_TOOLKIT_PACKAGE` 指定兼容工具包。

发行 ZIP 已解压到新的中文、空格路径，移除 Python / Conda / Qt 环境并仅保留 Windows PATH 后，
EXE 界面测试退出码为 0。检查了在线模型、离线预览及白色配置弹窗截图；
文件与产品版本均为 `1.2.2`，包内适配器及附带源码与本次代码直接逐字节比较一致。
ZIP 共 281 个条目，39,379,274 字节，没有包含用户配置、凭据或 Monitor 访问记录。

## v1.2.1：关闭后台与文件占用修复

2026-09-19，33 项自动测试通过。新增的实机回归使用临时 Node 进程与临时文件：

- 模拟连接记录已删除、子进程继承日志句柄的情况；停止后父子进程均退出，日志可以移动。
- 验证另一个项目的后台继续运行，其他位置的同名 `server.mjs` 不被当成 Monitor。
- 验证离线/旧版连接仍能使用本机停止入口。
- 验证退出桌面时可停止后台、保留后台或取消；停止失败时保持窗口打开。
- 网页关闭和 Codex 对话结束不会自动终止 Node 服务，该行为已在界面和使用说明中明确。

原 v1.1.0 工具包源代码继续保留；修复位于桌面适配器中。
Windows PowerShell 5.1 下另行通过 3 项进程发现、进程树清理与项目隔离检查。
v1.2.1 完整 ZIP 已解压到新的中文路径，在仅保留 Windows PATH 的环境中启动 EXE，
界面验证退出码为 0；文件版本、产品版本均为 `1.2.1`。

## v1.2.0 验证基线

验证日期：2026-09-19。执行方式：Codex 单代理；未发起真实 DSH 模型任务。

## 版本保留

- 原工具包基线：`825ff0d`，位于原工作目录和 `codex/installer-ui` 分支。
- 桌面版：独立 worktree，分支 `codex/desktop-console-v1.2.0`。
- 对比基线，`payload/`、`install/`、`installer/`、`uninstaller/` 源码没有修改。
- 桌面版号为 `1.2.0`；内附兼容工具包版本为 `1.1.0`。既有 GitHub 发布标签没有改动。

## 已完成验证

27 项自动测试通过，覆盖：

- 从用户选定目录读取默认模型，供应商顺序不改变选择；源设置保持原内容。
- 保存配置目录、项目去重、对话标记、损坏记录保护、文件写入失败反馈。
- HTTP 身份验证、项目与运行目录一致性、禁止远程地址与重定向、忽略代理。
- 同步前核对配置来源，模型偏好使用 revision 防止覆盖并发修改。
- 旧式 Monitor 提供只读地址概览，禁用配置与停止操作。
- 窗口持续响应、刷新保留未提交的模型选择、中文分段日志、进度和失败反馈。
- 在含中文与方括号的空白非 Git 项目中安装完整工具包，保留原有用户文件。
- Windows PowerShell 5.1 和 PowerShell 7 参数传递：使用所选配置目录，跳过模型调用，不打开额外浏览器。
- 使用临时 Node 服务验证进程发现、连接复用、精确停止；错误 PID 拒绝停止。
- Windows 当前用户 DPAPI 加解密验证，使用假 token。

另完成本机只读检查：发现两个运行中的 Monitor。旧版连接展示概览；新版连接成功读取任务与 DSH 会话。未修改或停止这些真实服务。

## 发行包验证

完整 ZIP：`dist/release-v1.2.0/codex-dsh-desktop-v1.2.0-windows-x64.zip`。

- 解压到新的中文、带空格目录，通过 EXE 的离线界面测试。
- 测试进程 PATH 仅含 Windows 目录，移除 Python、Conda、Qt 环境变量；退出码为 0。
- 工作台与配置页面截图检查通过，截图使用明确标记的示例数据。
- 确认包含 EXE、Qt 运行库、PowerShell 适配器、完整工具包、中文说明、桌面源码与许可证。
- 确认未包含用户 `settings.yaml`、凭据文件、桌面项目记录或 Monitor 访问记录。
- EXE 的文件版本、产品版本和窗口显示均为 `1.2.0`。
- 构建显式包含 Conda 的 ffi 依赖，并限制 DLL 搜索位置，避免混入其他软件的 ICU 库。

## 验证边界与复现

此次验证覆盖桌面适配器、现有服务读取和安装流程。真实供应商连通性、账号额度以及完整模型任务仍由用户的 DSH 配置决定；没有为验证界面消耗模型额度。

在项目声明的 Python 环境中执行：

```powershell
$env:DESKTOP_TEST_TOOLKIT_PACKAGE = '完整工具包目录'
python -m unittest discover -s desktop/tests -v
python desktop/build.py --toolkit-package '完整工具包目录' --output '新的输出目录'
.\CodexDshDesktop.exe --smoke-test '新的测试输出目录'
```

不设置 `DESKTOP_TEST_TOOLKIT_PACKAGE` 时，只跳过完整安装包测试。
