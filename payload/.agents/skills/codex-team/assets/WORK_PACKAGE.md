# 工作包（Work Package）

> 通用任务合同模板。填不完全的字段说明任务还没准备好派发。

## 身份

- Task：`<唯一标识>`
- 负责人：`<恰好一个>`
- 角色：`<协调者 / 顾问 / 探索者 / 执行者 / 测试者 / 审查者 / 记录者>`
- 依赖：`<必须先完成的任务或先取得的信息>`

## 目标

`<要改变的可观察状态，一到三句>`

## 验收标准

1. `<可判定的标准>`
2. `<可判定的标准>`

## 可写边界（allowlist）

- `<允许写入的路径>`

## 只读边界

- `<明确只读、不得修改的部分>`

## 需要的最小上下文

- `<相关文件、既有约定、已知坑>`

## 不在范围内

- `<最容易越界的那几项>`

## 交付要求

- 实际改动路径；
- 实际执行的验证与结果（命令、退出码、输出）；
- 未验证项与风险。
