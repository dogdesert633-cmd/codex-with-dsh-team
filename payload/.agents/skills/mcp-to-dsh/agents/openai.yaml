interface:
  display_name: "DSH 实时路由"
  short_description: "实时路由 DSH 代码角色并显示 session 与归属证据"
  default_prompt: "使用 $mcp-to-dsh 将这个明确角色的工作包交给真实 DSH，并显示实时 session、Git、测试和退出证据。"
policy:
  allow_implicit_invocation: true
